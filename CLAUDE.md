# CLAUDE.md

This repository keeps its canonical agent handbook in `AGENTS.md`.

If you are working in this checkout with Claude Code:

1. Read `AGENTS.md` first.
2. Treat `AGENTS.md` as the source of truth for build, test, render-host, and verification guidance.
3. If `CLAUDE.md` and `AGENTS.md` ever diverge, follow `AGENTS.md` and update this file only as a pointer to avoid drift.
